--[[ MyGame.Example.ArrayTable

  Automatically generated by the FlatBuffers compiler, do not modify.
  Or modify. I'm a message, not a cop.

  flatc version: 24.3.7

  Declared by  : //arrays_test.fbs
  Rooting type : MyGame.Example.ArrayTable (//arrays_test.fbs)

--]]

local __MyGame_Example_ArrayStruct = require('MyGame.Example.ArrayStruct')
local flatbuffers = require('flatbuffers')

local ArrayTable = {}
local mt = {}

function ArrayTable.New()
  local o = {}
  setmetatable(o, {__index = mt})
  return o
end

function ArrayTable.GetRootAsArrayTable(buf, offset)
  if type(buf) == "string" then
    buf = flatbuffers.binaryArray.New(buf)
  end

  local n = flatbuffers.N.UOffsetT:Unpack(buf, offset)
  local o = ArrayTable.New()
  o:Init(buf, n + offset)
  return o
end

function mt:Init(buf, pos)
  self.view = flatbuffers.view.New(buf, pos)
end

function mt:A()
  local o = self.view:Offset(4)
  if o ~= 0 then
    local x = self.view.pos + o
    local obj = __MyGame_Example_ArrayStruct.New()
    obj:Init(self.view.bytes, x)
    return obj
  end
end

function ArrayTable.Start(builder)
  builder:StartObject(1)
end

function ArrayTable.AddA(builder, a)
  builder:PrependStructSlot(0, a, 0)
end

function ArrayTable.End(builder)
  return builder:EndObject()
end

return ArrayTable